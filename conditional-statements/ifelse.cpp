#include<bits/stdc++.h>
using namespace std;
int main(){

  /*char ch;
  cin>>ch;
  if(ch>='A'&&ch<='Z'){
    cout<<"uppercase";

  }
  else if (ch>='a'&&ch<='z'){
    cout<<"no";
  }
  else if (ch>='0'&&ch<='9') {
    cout<<"digit";
  }

    return 0;*/

  // char ch;
  // cin>>ch;
  // if (isdigit(ch)) {
  //   cout<< "it is a digit"<<endl;
  //
  // }
  // else if (isupper(ch)) {
  //
  //   cout<< "it is a uppercase"<<endl;
  //
  // }
  // else if (islower(ch)) {
  //   cout<< "it is a lowercase"<<endl;
  }
  }